meta {
  name: List Consents
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/account/consents
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
