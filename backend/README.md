# Pequi — Backend (API)

Pacote Python do backend do Projeto Pequi (FastAPI, SQLAlchemy, Alembic).

Consulte o repositório raiz (`AGENTS.md`, `.agents/rules/`) para fluxo de desenvolvimento, testes (`scripts/run_tests.sh`) e convenções de API.
