# Code style — backend (Projeto Pequi)

Normas operacionais para o assistente no Cursor estão em **`.cursor/rules/pequi-backend-code-style.mdc`** (globs: `src/**/*.py`, `tests/**/*.py`, `alembic/**/*.py`).

Fonte canônica de contexto do repositório: **`AGENTS.md`** (cadeia de camadas, testes, LGPD, Bruno, CI, migrações).
